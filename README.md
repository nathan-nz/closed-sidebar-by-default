This simple Theme Component allows admins to control the default behaviour of the Discourse sidebar per group.

The most common use-case is a cleaner and simpler UI for casual visitors.

There is one setting, a simple group selector. It defaults to `anonymous_users` (ID 4).

---
For discussion about this component see the topic in Discourse Meta:

https://meta.discourse.org/t/closed-sidebar-by-default/408578
